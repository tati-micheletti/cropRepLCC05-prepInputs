Inside the module (prepInputsProblems) there are different maps and study areas using rasterToMatch argument from prepInputs. 
All cases where  rasterToMatch is used fail to return mySimOut or errors.
